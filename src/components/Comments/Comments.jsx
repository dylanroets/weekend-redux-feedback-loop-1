//will need useDispatch and useState
function Comments() {
  //want comment state to be captured the onChange input
  //want onClick function to:
  //INCLUDE INPUT VALIDATION
  //handle the dispatch(type='MAKE_COMMENT', payload: {supported: value, flagged: boolean})
  //THEN link to next page (review)

  return <p>Testing Comments</p>;
  // TODO: return text input, flagged, and Next button
}

export default Comments;
