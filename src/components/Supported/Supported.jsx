//will need useDispatch and useState
function Supported() {
  //want supported state to be captured the onChange input
  //want onClick function to:
  //INCLUDE INPUT VALIDATION
  //handle the dispatch(type='MAKE_COMMENT', payload: {supported: value})
  //THEN link to next page (understanding)

  return <p>Testing Supported</p>;
  // TODO: return input and Next button
}

export default Supported;
